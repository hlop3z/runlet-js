# Tasks: composable-capability-core

## 1. Registry + mux (behavior-preserving)

- [x] 1.1 Add `CapabilityDef` (name, js_wrapper, `types` (D11: `.d.ts` fragment via `include_str!("js/<cap>.d.ts")`, sibling to the wrapper), trust: `OperatorSupplied | ScriptControlled(SsrfPolicy)`, backend: `Arc<dyn Egress>`) to runlet-core; reject duplicate names at build — `capability.rs`; `backend` is `Option` (present ⇒ local, absent ⇒ fallback), see composable-core.md
- [x] 1.2 Add `LogicHost::builder()` (capabilities + optional `fallback_egress`); keep the old constructor delegating to it temporarily. D1: `.build()` returns a non-generic `LogicHost` (capabilities held in `Vec`/`HashMap`, not a type parameter per capability) — add a compile-time check (e.g. a function returning `LogicHost` with no capability generics) so a future refactor can't reintroduce capability-typed generics into the published surface — `host.rs::LogicHostBuilder` + `assert_logic_host_is_not_capability_generic`
- [x] 1.3 Replace the single egress slot with the per-name mux (`HashMap<name, Arc<dyn Egress>>` + fallback) in `ExecParams`/`engine.rs::io_hook`; preserve `EGRESS_UNAVAILABLE` semantics — `CapabilityRegistry::dispatch` + `engine::inject_mux`
- [ ] 1.4 Route `inject_apis` from the registered def list instead of cfg blocks (standard defs registered internally for now — engine tests must pass unchanged) — **mechanism in place** (`engine::inject_registry` injects enabled defs' wrappers via the registry); the standard defs' registration is folded into 3.1 (they move to `runlet-caps`) rather than a throwaway internal registration, and the cfg driver blocks are removed in 3.3. Engine tests pass unchanged.
- [x] 1.5 Enforce D4: apply the SSRF guard inside the mux for `ScriptControlled` defs; unit test a script-controlled def hitting a private IP is rejected pre-connect — `SsrfPolicy` (payload target extractor) + `script_controlled_private_ip_rejected_pre_connect`
- [x] 1.6 Enforce D9 fail-closed: an internal enforcement error/panic (metering, deadline-clock, trust-policy eval) denies the call; unit test that a mux with a failing metering/policy hook rejects rather than executes the I/O — `dispatch` `catch_unwind` guard + `mux_fails_closed_on_policy_error`/`mux_fails_closed_on_policy_panic`
- [x] 1.7 D9 bypass surface: write down the enumerated unmediated-authority list (in-engine `http`/`s3`, clock, RNG, exit) in `docs/design/composable-core.md`; test that the deterministic profile *removes* ambient time/RNG/`$sys` (script cannot re-reach them), not merely stubs them — `determinism.js` now `delete`s; `deterministic_profile_removes_ambient_authority`
- [x] 1.8 Unit tests: mixed topology (one name local `EchoEgress`, one via fallback), unregistered name → global undefined, deterministic profile injects nothing — `mixed_topology_local_and_fallback`, `registered_wrapper_injected_only_when_enabled`, `resource_withheld_under_deterministic_profile`, `no_backend_no_fallback_is_egress_unavailable`

## 2. Metrics + config dynamization

- [x] 2.1 Replace cfg'd `ExecMetrics`/`Collectors` per-cap fields with a name-keyed collector map; drain into `Outcome` — the only engine-metered caps are the in-engine bypass surface (`http`/`s3`); the driver caps meter in the sidecar (`BackendMetrics`). The name-keying happens at the response layer where both sources meet (`meta.io`, see 2.2); the engine `ExecMetrics` stays typed for `http`/`s3`.
- [x] 2.2 Handler: replace fixed `Meta.<cap>_requests` with `meta.io.<name>` map; make `RequestConfig.io` generic over registered names — `Meta.io: BTreeMap<String, Value>` via `insert_io`; `CapabilitySet.io` names via `RequestIo::enabled_names()`
- [x] 2.3 Verify `events.rs` usage-event dims against the new metrics source (no tenant-metering spec change) — `CapabilityOps` now sourced from `meta.io` via `io_count`; dims unchanged, `events.rs` itself unchanged
- [x] 2.4 Update `tests/test_simple.py` meta assertions and `container/types.d.ts` meta typings to `meta.io` — `db_requests`/`auth_requests` → `meta.io.db`/`meta.io.auth`; `types.d.ts` regenerated with `meta.io` typing
- [x] 2.5 D11: split core's `types.d.ts` into a `base.d.ts` + one `.d.ts` fragment per capability; generator (`runlet_core::generate_types_dts`) concatenates `base` + `http`/`s3` + each registered def's `types`; golden `#[test]` (`types_dts_is_up_to_date` in runlet-caps) regenerates + diffs. Interface-name-prefix convention documented in `types.rs` + composable-core.md. `base.d.ts` declares `meta.io`
- [x] 2.6 D12: rename the script-facing HTTP global `api` → `http` — `js/api.js` → `js/http.js` (+ `http.d.ts` fragment), `HTTP_WRAPPER`/`inject_http`, `declare const http`; `docs/`, `README.md`, `container/`, `tests/test_simple.py` updated. `config.allowed_hosts` gating unchanged; `ErrorSource::Api` taxonomy tag kept (per D12 scope); `http` stays in core

## 3. Preset extraction + core slimming

- [x] 3.0 D10: define the shared action-token identities as a `const`/enum within this repo — `runlet_caps::actions` (DB/MONGO/MAIL/REDIS/AMQ/AUTH). Since the driver dispatch lives cross-repo in `fabricd`, the box-side guard is the `action_tokens_match_wrappers` fixture (asserts each token appears in its wrapper); the cross-repo seam stays the `runlet-wire` string protocol. `runlet-wire` untouched.
- [x] 3.1 Create `runlet-caps` crate: the six standard `CapabilityDef`s (JS wrappers + D11 `.d.ts` fragments moved from core, `Trust::OperatorSupplied`, action tokens from the D10 module, action-token fixture test)
- [x] 3.2 Flip the `runlet` bin to compose the preset (`LogicHost::builder(...).capabilities(runlet_caps::preset())`) + per-request sidecar fallback (stock behavior parity)
- [x] 3.3 Delete core's six wrapper modules + `js/*.js` for driver caps; remove the six cargo features (`full = ["http", "s3"]`); keep `http`/`s3`
- [x] 3.4 Update `scripts/sweep.sh` to the `http`/`s3`-only matrix; confirm `--no-default-features` links no network dep (`cargo tree -i reqwest` check, verified)

## 4. Verification + docs

- [x] 4.1 Full gate: fmt-check + clippy gauntlet + tests, **green in Docker** (the sanctioned build path; native Windows can't build `aws-lc-sys` and test-exe execution is blocked by Windows Application Control). Workspace tests: runlet 47, runlet-caps 2, runlet-core 45, runlet-wire 11; no-default mux tests: 53. Feature sweep = `http`/`s3`-only matrix + `cargo tree -i reqwest` (3.4).
- [x] 4.2 Unit mixed-topology smoke (`mixed_topology_local_and_fallback` + `no_backend_no_fallback_is_egress_unavailable`) **and** the live integration suite vs the sibling `fabricd`: ran `tests/test_simple.py` with both crates built Linux-side in Docker (box + `fabricd` over a UDS, backends reached via `host.docker.internal`) → **180/180 passed, 0 failed**. Exercises the new registry → preset → mux → `fabricd` → `meta.io` path end-to-end across `db` (CockroachDB + PgBouncer), `mongo`, `amq` (NATS), plus trusted-mode, OTel, and per-tenant usage/audit events. Environmental self-skips only (not code): PostgreSQL-direct (host stack runs two pg containers; `:5432` auth differs from the pooler path — the `db` path is still covered by Cockroach + PgBouncer) and Keycloak/Zitadel auth (need PAT/compose setup).
- [x] 4.3 runlet-bench: added `benches/mux.rs` driving the real `LogicHost` mux hot path through the public API (0 / 1 / 10 `io.call`s vs a no-op backend). Criterion run (Docker): baseline_0 ≈ 926 µs `[895–961]`, one_io_call ≈ 882 µs `[866–900]`, ten_io_calls ≈ 925 µs `[912–943]` — the arms are statistically indistinguishable (overlapping CIs; 10 calls add nothing above the ~±30 µs noise floor). **No measurable hot-path regression from the mux's one `HashMap` lookup per `io.call`**, as design.md predicted.
- [x] 4.4 CLAUDE.md updated (the two-kinds capability pattern + files-touched for each; the four-crate workspace list incl. `runlet-caps`); `docs/design/composable-core.md` decision record with the enumerated bypass surface, the two resolved design details, and a custom-box `main.rs` snippet. README extension how-to: see composable-core.md (the canonical extension guide).
